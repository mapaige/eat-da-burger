<div class="container" class="text-center ">
  <div class="row row-col-4 bg-white text-warning " id="burgers">
    <div class="col ">
      <input type="text" id="newBurger" name="burger_name"> <button class="addBurger text-warning" data-id="{{id}}">Add
        Burger</button>
    </div>






  
        <div class="col" id="burgerDevoured">
          <h4>Burgers to Devour!</h4>
          <ul class="list-unstyled">
            {{#each burgerData}}
            {{#unless devoured}}
            <li>
              <p>{{burger_name}} <button data-id={{id}} class="devouredBtn text-warning">Devoured</button></p>
            </li>
            {{/unless}}
            {{/each}}
          </ul>
        </div>
     



     
        <div class="col">
          <ul class="list-unstyled">
            {{#each burgerData}}
            {{#if devoured}}
            <li>
              <p>{{burger_name}} </p>
            </li>
            {{/if}}
            {{/each}}
          </ul>
        </div>
    
    </div>
</div>